/**
 * Mission Timeline — Phase 5 feature.
 *
 * Will show a temporal view of all mission events, decisions,
 * and state changes. Used for replay and post-mission review.
 */
export function MissionTimeline() {
  return (
    <div className="p-4 text-center text-aegis-text-muted text-xs font-mono">
      Timeline view — coming in Phase 5
    </div>
  );
}
